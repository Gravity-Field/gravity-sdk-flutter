# Analyzer & linter configuration for the example app.
# Mirrors the strictness of the root gravity_sdk package and adds a curated set
# of rules. Docs: https://dart.dev/lints
include: package:flutter_lints/flutter.yaml

# Keep the same formatter behaviour as the root package.
formatter:
  trailing_commas: preserve

analyzer:
  exclude:
    # Generated code (flutter_gen, build_runner) — never lint these.
    - "lib/gen/**"
    - "**/*.g.dart"
    - "**/*.gr.dart"
    - "**/*.gen.dart"
    - "**/*.config.dart"
    - "**/*.freezed.dart"
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    # const / immutability
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true

    # style
    prefer_single_quotes: true
    require_trailing_commas: true
    directives_ordering: true
    sort_pub_dependencies: true
    use_super_parameters: true
    always_declare_return_types: true

    # widgets
    sized_box_for_whitespace: true
    use_decorated_box: true
    use_colored_box: true

    # correctness / clarity
    avoid_redundant_argument_values: true
    unawaited_futures: true
    cascade_invocations: true
    unnecessary_parenthesis: true
    unnecessary_lambdas: true
    avoid_void_async: true
    prefer_final_locals: true
