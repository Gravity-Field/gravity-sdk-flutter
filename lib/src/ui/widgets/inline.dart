import 'package:flutter/material.dart';

class GravityInlineWidget extends StatelessWidget {
  const GravityInlineWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
